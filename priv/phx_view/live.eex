defmodule <%= webapp_name %>.<%= controller %>Live do

  use Phoenix.LiveView
  alias <%= webapp_name %>.<%= controller %>View

  def render(assigns) do
    <%= controller %>View.render("index.html", assigns)
  end

  def mount(_session, socket) do
    {:ok, socket}
  end

end
